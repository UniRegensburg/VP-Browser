/* eslint-env browser */
/* global Vue, VueRouter */

var VPModel = VPModel || {};
VPApp.VPModel = function() {
  "use strict";

  var that = {};

  function initVPModel() {
    console.log("VPModel")
  }

  that.initVPModel = initVPModel;
  return that;
};
