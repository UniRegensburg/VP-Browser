/* eslint-env browser */
/* global Vue, VueRouter */

var VPView = VPView || {};
VPApp.VPView = function() {
  "use strict";

  var that = {};

  function initVPView() {
    console.log("VPView")
  }

  that.initVPView = initVPView;
  return that;
};
